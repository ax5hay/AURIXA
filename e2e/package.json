{
  "name": "@aurixa/e2e",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "playwright test --config=playwright.config.ts",
    "test:smoke": "playwright test smoke --config=playwright.config.ts",
    "test:visual": "playwright test visual --config=playwright.config.ts",
    "update-snapshots": "playwright test visual --config=playwright.config.ts --update-snapshots",
    "demo-videos": "playwright test demo-videos --config=playwright.demo-videos.config.ts",
    "screenshots": "playwright test screenshots --config=playwright.screenshots.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
